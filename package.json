{
  "name": "hotel-room-allocation-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node scripts/test-client-sync.js && node scripts/test-state.js && node scripts/test-state-api.js && node scripts/test-overview-workbook.js && node scripts/test-security.js",
    "build": "node scripts/build.js",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build",
    "maintenance:refresh": "node scripts/refresh-views.js",
    "backup:file": "node scripts/backup-to-file.js",
    "backup:verify": "node scripts/verify-backup.js"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  },
  "overrides": {
    "uuid": "11.1.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/node": "^22.20.1",
    "eslint": "^9.39.5",
    "globals": "^15.15.0",
    "typescript": "^5.9.3"
  }
}
